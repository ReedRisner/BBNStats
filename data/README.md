# Data directory

`data/update.json` is generated by `scripts/fetch_daily_stats.py`.

## Automated refresh

A scheduled GitHub Action (`.github/workflows/daily-stats-sync.yml`) regenerates this file daily and commits it when values change.

You can also run it locally:

```bash
python3 scripts/fetch_daily_stats.py
```
